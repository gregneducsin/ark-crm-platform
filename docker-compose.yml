services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: ark_health
      POSTGRES_USER: ark
      POSTGRES_PASSWORD: localdev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ark -d ark_health"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://ark:localdev@db:5432/ark_health
      PORT: "3001"
      NODE_ENV: development
      CORS_ALLOWED_ORIGINS: http://localhost:5173
    env_file:
      - path: .env
        required: false
    ports:
      - "3001:3001"

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    depends_on:
      api:
        condition: service_healthy
    environment:
      API_UPSTREAM: api:3001
      NGINX_RESOLVER: "127.0.0.11 valid=1s"
    ports:
      - "5173:80"

volumes:
  pgdata:
